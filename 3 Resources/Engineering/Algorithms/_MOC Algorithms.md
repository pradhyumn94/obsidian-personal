---
type: moc
tags: [moc, dsa]
---

# Algorithm Patterns — MOC

One note per pattern (two pointers, sliding window, DP on trees, etc.), template: `Algorithm Pattern`. Each pattern note links back to the DSA problems that used it.

```dataview
TABLE category, status
FROM "3 Resources/Engineering/Algorithms"
WHERE type = "algorithm-pattern"
SORT category ASC
```
